"use client";

import Image from "next/image";
import dynamic from "next/dynamic";
import { useEffect, useState } from "react";

const PaperReader = dynamic(() => import("./paper-reader"), {
  ssr: false,
  loading: () => <div className="grid h-[62vh] place-items-center rounded-xl bg-[#302920] text-sm font-black text-amber-100/50 lg:h-[calc(100vh-9rem)]">PAPER READER LOADING...</div>,
});

type OrderCard = { id: string; name: string; role: string };

function shuffleCards(cards: OrderCard[]) {
  const shuffled = [...cards];
  for (let index = shuffled.length - 1; index > 0; index -= 1) {
    const randomIndex = Math.floor(Math.random() * (index + 1));
    [shuffled[index], shuffled[randomIndex]] = [shuffled[randomIndex], shuffled[index]];
  }
  if (shuffled.length > 1 && shuffled.every((card, index) => card.id === cards[index].id)) {
    [shuffled[0], shuffled[1]] = [shuffled[1], shuffled[0]];
  }
  return shuffled;
}
type ThirdQuestion = { heading: string; prompt: string; type: "single" | "multi"; options: { id: string; name: string; note: string }[]; correct: string[] };
type ChapterTwoMission = {
  id: number; title: string; paperTitle: string; file: string; topic: string;
  orderHeading: string; orderAccent: string; orderInstruction: string; cards: OrderCard[];
  keywords: string[]; missingKeyword: string; third: ThirdQuestion;
};

const STRUCTURE_ONE: OrderCard[] = [
  { id: "title", name: "Title & Authors", role: "연구 주제와 저자 정보" }, { id: "abstract", name: "Abstract & Keywords", role: "연구 전체 요약과 핵심어" },
  { id: "intro", name: "Introduction", role: "문제, 필요성, 연구 목표" }, { id: "related", name: "Related Work", role: "기존 연구와 이 논문의 차이" },
  { id: "background", name: "Background", role: "이해에 필요한 기술적 배경" }, { id: "fileops", name: "File Operations in Secure Container Runtimes", role: "각 런타임의 파일 I/O 동작 구조" },
  { id: "performance", name: "Performance and CPU Usage Analysis", role: "실험 설정, 성능과 CPU 사용 결과" }, { id: "bottleneck", name: "Bottleneck Analysis Using Symbol-Level Profiling", role: "성능 차이의 근본 원인 분석" },
  { id: "discussion", name: "Discussion", role: "결과의 의미, 선택 기준과 향후 과제" }, { id: "conclusion", name: "Concluding Remarks", role: "핵심 발견과 기여 정리" },
  { id: "references", name: "References", role: "인용한 연구와 자료 목록" },
];
const STRUCTURE_FOUR: OrderCard[] = [
  { id: "title", name: "Title & Authors", role: "연구 제목과 저자 정보" }, { id: "abstract", name: "Abstract & Keywords", role: "연구 전체 요약과 핵심어" },
  { id: "intro", name: "Introduction", role: "교육 문제와 CodeDive의 연구 목표" }, { id: "related", name: "Related Work", role: "기존 web IDE와 activity tracking 연구" },
  { id: "design", name: "System Design", role: "CodeDive의 전체 구조와 주요 구성 요소" }, { id: "workflows", name: "Workflows", role: "학생과 교수자의 사용 흐름" },
  { id: "implementation", name: "Implementation Details", role: "deployment, monitoring, authentication 구현" }, { id: "results", name: "Implementation Results", role: "실제 수업 배포 결과와 수집 데이터" },
  { id: "discussion", name: "Discussion", role: "결과의 의미, 한계와 향후 과제" }, { id: "conclusions", name: "Conclusions", role: "핵심 기여와 연구 결론" },
  { id: "references", name: "References", role: "인용한 연구와 자료" },
];

export const CHAPTER_TWO_MISSIONS: ChapterTwoMission[] = [
  { id: 1, title: "논문 구조 순서 맞추기", paperTitle: "Impact of Secure Container Runtimes on File I/O Performance in Edge Computing", file: "/papers/mission1.pdf", topic: "Paper Structure", orderHeading: "논문의 구조를 완성해 보세요", orderAccent: "Paper Structure", orderInstruction: "paper의 큰 section heading을 확인하고 실제 등장 순서대로 배치하세요.", cards: STRUCTURE_ONE, keywords: ["container runtimes", "GPU scheduling", "I/O performance", "security", "kernel-level profiling"], missingKeyword: "GPU scheduling", third: { heading: "이 논문에서 비교하지 않은 대상은?", prompt: "성능 비교에 포함되지 않은 기술을 선택하세요.", type: "single", correct: ["kubernetes"], options: [{ id: "kata", name: "Kata Containers", note: "secure container runtime" }, { id: "gvisor", name: "gVisor", note: "sandboxed container runtime" }, { id: "firecracker", name: "Firecracker", note: "microVM runtime" }, { id: "kubernetes", name: "Kubernetes", note: "container orchestration" }] } },
  { id: 2, title: "Abstract 핵심 찾기", paperTitle: "Kafe: Can OS Kernels Forward Packets Fast Enough for Software Routers?", file: "/papers/mission2.pdf", topic: "Abstract", orderHeading: "Abstract의 흐름을 완성해 보세요", orderAccent: "핵심 논리", orderInstruction: "연구가 전개되는 흐름대로 카드를 배치하세요.", cards: [{ id: "problem", name: "기존 인식과 문제", role: "범용 OS 기반 software router는 빠른 packet 처리가 어렵다는 인식" }, { id: "question", name: "연구 질문", role: "kernel network stack을 다시 설계하면 한계를 극복할 수 있는가?" }, { id: "solution", name: "제안 시스템", role: "kernel 기반 packet forwarding engine인 Kafe 제안" }, { id: "result", name: "평가 결과", role: "Linux보다 7배 빠르고 DPDK와 비슷한 성능을 더 적은 자원으로 달성" }], keywords: ["Software router", "OS network stack", "Optimization", "Packet forwarding"], missingKeyword: "Packet forwarding", third: { heading: "Kafe가 제안한 새로운 기술 2개는?", prompt: "새로운 mechanism으로 소개된 두 항목을 선택하세요.", type: "multi", correct: ["skb", "flow"], options: [{ id: "skb", name: "skb Stack", note: "pre-allocated skb를 cache-friendly 방식으로 재사용" }, { id: "flow", name: "Flow Repository", note: "같은 flow의 kernel object를 packet 사이에서 cache" }, { id: "rss", name: "Receive Side Scaling", note: "NIC의 기존 packet 분산 기능" }, { id: "napi", name: "NAPI polling", note: "Linux driver의 기존 polling 방식" }] } },
  { id: 3, title: "Figure 먼저 읽기", paperTitle: "CLIK: Cloud-based Linux kernel practice environment and judgment system", file: "/papers/mission3.pdf", topic: "Figures", orderHeading: "CLIK 연구 흐름을 완성해 보세요", orderAccent: "문제에서 결과까지", orderInstruction: "연구 배경부터 실제 결과까지 순서대로 배치하세요.", cards: [{ id: "context", name: "교육적 배경", role: "실제 OS 이해를 위해 kernel programming assignment가 필요함" }, { id: "difficulty", name: "학습과 평가의 어려움", role: "환경 구축과 reboot 평가에 많은 시간이 필요함" }, { id: "solution", name: "제안 시스템", role: "cloud 기반 Linux kernel 실습 환경과 자동 평가 시스템 CLIK" }, { id: "evidence", name: "수업 적용 결과", role: "40명의 환경을 지원하고 parallel judgment로 1분 이내 평가" }], keywords: ["Automatic judgment system", "Cloud system", "Linux kernel assignment", "Operating systems", "Container orchestration"], missingKeyword: "Container orchestration", third: { heading: "CLIK는 무엇으로 구축되었을까요?", prompt: "실제 private cloud 구축에 사용한 platform을 선택하세요.", type: "single", correct: ["openstack"], options: [{ id: "openstack", name: "OpenStack", note: "open-source cloud infrastructure platform" }, { id: "cloudstack", name: "Apache CloudStack", note: "또 다른 cloud software" }, { id: "aws", name: "Amazon Web Services", note: "commercial public cloud" }, { id: "gcp", name: "Google Cloud Platform", note: "commercial public cloud" }] } },
  { id: 4, title: "한 문장으로 요약하기", paperTitle: "CodeDive: A Web-Based IDE with Real-Time Code Activity Monitoring for Programming Education", file: "/papers/mission4.pdf", topic: "Summary", orderHeading: "논문의 구조를 완성해 보세요", orderAccent: "CodeDive Paper Outline", orderInstruction: "paper의 큰 section heading을 실제 등장 순서대로 배치하세요.", cards: STRUCTURE_FOUR, keywords: ["Integrated Development Environment", "Programming education", "Activity monitoring", "Packet routing"], missingKeyword: "Packet routing", third: { heading: "CodeDive deployment는 어떤 platform 기반일까요?", prompt: "scalable cloud architecture의 기반 platform을 선택하세요.", type: "single", correct: ["kubernetes"], options: [{ id: "kubernetes", name: "Kubernetes", note: "container orchestration 기반 cloud architecture" }, { id: "swarm", name: "Docker Swarm", note: "Docker clustering platform" }, { id: "mesos", name: "Apache Mesos", note: "distributed resource platform" }, { id: "openshift", name: "OpenShift", note: "enterprise container platform" }] } },
];

type ChapterTwoProps = { userId: string; chapterCompleted: boolean; onBackToLabLog: () => void; onUnlocked: () => Promise<void> };
export default function ChapterTwo({ userId, chapterCompleted, onBackToLabLog, onUnlocked }: ChapterTwoProps) {
  const [view, setView] = useState<"loading" | "intro" | "map" | "mission" | "complete">("loading");
  const [completed, setCompleted] = useState<number[]>([]);
  const [selectedMission, setSelectedMission] = useState(1);
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState("");

  useEffect(() => {
    const timer = window.setTimeout(() => {
      const raw = window.localStorage.getItem(`labquest-chapter2-${userId}`);
      const progress = raw ? (JSON.parse(raw) as number[]) : [];
      setCompleted(progress.filter((value) => [1, 2, 3, 4].includes(value)));
      setView(chapterCompleted ? "complete" : progress.length ? "map" : "intro");
    }, 0);
    return () => window.clearTimeout(timer);
  }, [chapterCompleted, userId]);

  async function finishMission(number: number) {
    const next = Array.from(new Set([...completed, number])).sort();
    setCompleted(next);
    window.localStorage.setItem(`labquest-chapter2-${userId}`, JSON.stringify(next));
    if (number !== 4) { setView("map"); return; }
    setSaving(true); setError("");
    try { await onUnlocked(); setView("complete"); }
    catch { setError("Chapter 2 완료 상태를 저장하지 못했습니다. 다시 시도해 주세요."); setView("map"); }
    finally { setSaving(false); }
  }

  if (view === "loading") return <C2Shell><p className="m-auto text-sm font-black tracking-widest text-amber-200">CHAPTER 02 LOADING...</p></C2Shell>;
  if (view === "intro") return <C2Intro onBack={onBackToLabLog} onStart={() => setView("map")} />;
  if (view === "complete") return <C2Complete onMap={() => setView("map")} onProfile={onBackToLabLog} />;
  if (view === "mission") return <PaperMission mission={CHAPTER_TWO_MISSIONS[selectedMission - 1]} onBack={() => setView("map")} onComplete={() => finishMission(selectedMission)} saving={saving} />;
  return <C2Map completed={completed} error={error} onBack={onBackToLabLog} onPlay={(number) => { setSelectedMission(number); setView("mission"); }} />;
}

function C2Intro({ onBack, onStart }: { onBack: () => void; onStart: () => void }) {
  return <C2Shell><button onClick={onBack} className="absolute left-5 top-5 rounded-full border border-white/15 bg-white/[.05] px-4 py-2 text-sm font-bold text-white/60">← LABLOG</button><section className="mx-auto grid max-w-5xl items-center gap-10 pt-16 lg:grid-cols-[.75fr_1.25fr]"><div className="relative mx-auto h-64 w-52 rotate-[-4deg] rounded-lg bg-[#f6ecd0] p-7 text-[#332819] shadow-[18px_22px_0_rgba(50,34,18,.25)]"><small className="text-xs font-black tracking-[.2em]">RESEARCH</small><strong className="mt-5 block font-serif text-5xl">PAPER</strong><span className="absolute bottom-7 left-7 text-sm font-black">CHAPTER 02</span></div><div><p className="text-xs font-black tracking-[.2em] text-amber-300">OS LAB · NEW JOURNEY</p><h1 className="mt-4 text-6xl font-black tracking-[-.06em] sm:text-8xl">논문<br /><em className="not-italic text-[#f2c66d]">익숙하기</em></h1><p className="mt-6 text-base font-semibold leading-8 text-white/60">논문을 전부 읽으려고 애쓰지 않아도 괜찮아요.<br />먼저 구조를 보고, 중요한 부분을 찾는 법부터 시작해요.</p><div className="mt-7 rounded-2xl border border-amber-200/20 bg-amber-200/[.06] p-5"><small className="text-[10px] font-black tracking-widest text-amber-200">CHAPTER OBJECTIVE</small><strong className="mt-2 block text-lg">논문의 구조를 이해하고 핵심 내용을 내 언어로 설명하기</strong></div><button onClick={onStart} className="mt-7 rounded-full bg-[#f2c66d] px-8 py-4 text-base font-black text-[#302416] shadow-[0_7px_0_#806126]">CHAPTER 2 시작 →</button></div></section></C2Shell>;
}

function C2Map({ completed, error, onBack, onPlay }: { completed: number[]; error: string; onBack: () => void; onPlay: (number: number) => void }) {
  return <C2Shell><div className="mx-auto max-w-5xl"><header className="flex items-center justify-between"><button onClick={onBack} className="rounded-full bg-white/[.06] px-4 py-2 text-sm font-bold text-white/60">← LABLOG</button><span className="rounded-full border border-amber-200/20 bg-amber-200/[.05] px-4 py-2 text-sm font-black text-amber-200">{completed.length} / 4 COMPLETE</span></header><div className="mt-10"><p className="text-xs font-black tracking-[.2em] text-amber-300">CHAPTER 02 · PAPER FAMILIARITY</p><h1 className="mt-3 text-5xl font-black sm:text-7xl">논문 탐험 지도</h1><p className="mt-4 text-base text-white/50">각 Mission에는 서로 다른 OS paper가 연결되어 있어요.</p></div>{error && <p className="mt-5 rounded-xl bg-red-300/10 p-4 text-sm font-bold text-red-200">{error}</p>}<div className="mt-9 grid gap-4 md:grid-cols-2">{CHAPTER_TWO_MISSIONS.map((mission) => { const unlocked = mission.id === 1 || completed.includes(mission.id - 1); const done = completed.includes(mission.id); return <button key={mission.id} disabled={!unlocked} onClick={() => onPlay(mission.id)} className={`group relative overflow-hidden rounded-[1.5rem] border p-6 text-left transition ${done ? "border-emerald-300/30 bg-emerald-300/[.07]" : unlocked ? "border-amber-200/20 bg-[#2a241c] hover:-translate-y-1 hover:border-amber-200/50" : "border-white/5 bg-white/[.02] opacity-40"}`}><div className="flex items-start justify-between"><span className={`grid h-12 w-12 place-items-center rounded-xl font-black ${done ? "bg-emerald-300 text-emerald-950" : "bg-amber-200/10 text-amber-200"}`}>{done ? "✓" : unlocked ? mission.id : "🔒"}</span><b className="text-xs text-white/35">{unlocked ? done ? "REPLAY →" : "PLAY →" : "LOCKED"}</b></div><small className="mt-6 block text-[10px] font-black tracking-[.16em] text-amber-300">MISSION {String(mission.id).padStart(2, "0")} · {mission.topic}</small><h2 className="mt-2 text-2xl font-black">{mission.title}</h2><p className="mt-3 line-clamp-2 text-sm leading-6 text-white/45">{mission.paperTitle}</p></button>; })}</div></div></C2Shell>;
}

function PaperMission({ mission, onBack, onComplete, saving }: { mission: ChapterTwoMission; onBack: () => void; onComplete: () => Promise<void>; saving: boolean }) {
  const [step, setStep] = useState(1); const [outline, setOutline] = useState<string[]>([]); const [orderState, setOrderState] = useState<"idle" | "wrong" | "correct">("idle"); const [keyword, setKeyword] = useState<string | null>(null); const [thirdSelected, setThirdSelected] = useState<string[]>([]); const [thirdChecked, setThirdChecked] = useState(false);
  const [scrambled, setScrambled] = useState<OrderCard[]>(() => [...mission.cards].reverse());
  useEffect(() => {
    const timer = window.setTimeout(() => setScrambled(shuffleCards(mission.cards)), 0);
    return () => window.clearTimeout(timer);
  }, [mission.cards]);
  const bank = scrambled.filter((card) => !outline.includes(card.id));
  const keywordCorrect = keyword === mission.missingKeyword;
  const thirdCorrect = thirdChecked && mission.third.correct.length === thirdSelected.length && mission.third.correct.every((id) => thirdSelected.includes(id));
  function move(index: number, delta: number) { const target = index + delta; if (target < 0 || target >= outline.length) return; setOutline((current) => { const next = [...current]; const [item] = next.splice(index, 1); next.splice(target, 0, item); return next; }); setOrderState("idle"); }
  function checkOrder() { setOrderState(outline.length === mission.cards.length && outline.every((id, index) => id === mission.cards[index].id) ? "correct" : "wrong"); }
  function resetOrder() { setOutline([]); setOrderState("idle"); setScrambled(shuffleCards(mission.cards)); }
  function toggleThird(id: string) { setThirdChecked(false); if (mission.third.type === "single") setThirdSelected([id]); else setThirdSelected((current) => current.includes(id) ? current.filter((item) => item !== id) : current.length < mission.third.correct.length ? [...current, id] : current); }
  return <main className="min-h-screen bg-[#17130f] text-white"><header className="grid min-h-20 grid-cols-[auto_1fr_auto] items-center gap-4 border-b border-amber-100/15 bg-[#100d0a] px-4 sm:px-8"><button onClick={onBack} className="rounded-full bg-white/[.06] px-4 py-2 text-sm font-bold text-white/60">← 논문 지도</button><div><small className="text-[10px] font-black tracking-[.16em] text-amber-300">CHAPTER 02 · MISSION {String(mission.id).padStart(2, "0")}</small><strong className="mt-1 block text-base">{mission.title}</strong></div><span className="text-sm font-black text-amber-200">0{step} / 03</span></header><section className="grid min-h-[calc(100vh-5rem)] lg:grid-cols-[1.05fr_.95fr]"><div className="border-b border-amber-100/15 bg-[#211c16] p-4 lg:border-b-0 lg:border-r"><div className="mb-3"><small className="text-[10px] font-black tracking-widest text-amber-300">RESEARCH PAPER · MISSION {mission.id}</small><strong className="mt-1 block truncate text-sm text-white/70">{mission.paperTitle}</strong></div><PaperReader file={mission.file} title={mission.paperTitle} /></div><aside className="max-h-[calc(100vh-5rem)] overflow-y-auto p-5 sm:p-8">{step === 1 ? <><QuestionHeading eyebrow="QUESTION 01 · RESEARCH FLOW" title={mission.orderHeading} accent={mission.orderAccent} description={mission.orderInstruction} /><div className={`mt-6 rounded-2xl border p-4 ${orderState === "correct" ? "border-emerald-300/40 bg-emerald-300/[.06]" : orderState === "wrong" ? "border-red-300/40 bg-red-300/[.06]" : "border-amber-100/15 bg-white/[.03]"}`}><div className="flex justify-between text-xs font-black text-white/40"><span>YOUR PAPER OUTLINE</span><b>{outline.length} / {mission.cards.length}</b></div><div className="mt-3 max-h-72 space-y-2 overflow-y-auto">{outline.length === 0 && <p className="p-5 text-center text-sm text-white/30">아래 카드를 순서대로 선택하세요.</p>}{outline.map((id, index) => { const card = mission.cards.find((item) => item.id === id)!; return <div key={id} className="grid grid-cols-[2rem_1fr_auto] items-center gap-2 rounded-xl bg-[#f4ead1] p-3 text-[#352a1e]"><b className="text-sm">{index + 1}</b><span><strong className="block text-sm">{card.name}</strong><small className="text-xs opacity-60">{card.role}</small></span><span className="flex gap-1"><button onClick={() => move(index, -1)} className="rounded bg-black/10 px-2 py-1">↑</button><button onClick={() => move(index, 1)} className="rounded bg-black/10 px-2 py-1">↓</button><button onClick={() => { setOutline((current) => current.filter((item) => item !== id)); setOrderState("idle"); }} className="rounded bg-red-500/10 px-2 py-1">×</button></span></div>; })}</div></div><div className="mt-4"><small className="text-[10px] font-black tracking-widest text-amber-300">PAPER SECTIONS</small><div className="mt-2 grid gap-2">{bank.map((card) => <button key={card.id} onClick={() => { setOutline((current) => [...current, card.id]); setOrderState("idle"); }} className="flex items-center justify-between rounded-xl border border-amber-100/15 bg-white/[.035] p-3 text-left hover:border-amber-200/40"><span><strong className="block text-sm">{card.name}</strong><small className="text-xs text-white/40">{card.role}</small></span><b className="text-amber-200">＋</b></button>)}</div></div><div className="mt-4 rounded-xl bg-white/[.04] p-4 text-sm font-semibold text-white/60">{orderState === "correct" ? "✓ 핵심 흐름을 완성했습니다!" : orderState === "wrong" ? "순서가 아직 맞지 않아요. paper의 heading을 다시 확인하세요." : "요약 → 문제 → 방법 → 결과 → 결론의 흐름을 찾아보세요."}</div><div className="mt-4 grid grid-cols-2 gap-2"><button onClick={resetOrder} className="rounded-full border border-white/15 p-3 text-sm font-bold">다시 시작</button>{orderState === "correct" ? <button onClick={() => setStep(2)} className="rounded-full bg-amber-300 p-3 text-sm font-black text-[#342612]">다음 문제 →</button> : <button disabled={outline.length !== mission.cards.length} onClick={checkOrder} className="rounded-full bg-amber-300 p-3 text-sm font-black text-[#342612] disabled:opacity-30">제출</button>}</div></> : step === 2 ? <><QuestionHeading eyebrow="QUESTION 02 · KEYWORD" title="이 논문에 없는 keyword는?" accent="Keyword 찾기" description="첫 페이지의 Keywords 또는 Index Terms를 확인하고 하나를 선택하세요." /><div className="mt-7 grid gap-3">{mission.keywords.map((option) => { const selected = keyword === option; return <button key={option} onClick={() => setKeyword(option)} className={`flex items-center gap-3 rounded-2xl border p-4 text-left text-base font-bold ${selected ? keywordCorrect ? "border-emerald-300 bg-emerald-300/10 text-emerald-200" : "border-red-300 bg-red-300/10 text-red-200" : "border-white/15 bg-white/[.035]"}`}><i className="grid h-8 w-8 place-items-center rounded-full bg-white/10 not-italic">{selected ? keywordCorrect ? "✓" : "×" : ""}</i>{option}</button>; })}</div>{keyword && <div className={`mt-5 rounded-xl p-4 text-sm font-bold ${keywordCorrect ? "bg-emerald-300/10 text-emerald-200" : "bg-red-300/10 text-red-200"}`}>{keywordCorrect ? "정답입니다! 이 항목은 paper의 keyword가 아닙니다." : "이 keyword는 paper에 있어요. 다시 확인해 보세요."}</div>}<div className="mt-5 grid grid-cols-2 gap-2"><button onClick={() => setStep(1)} className="rounded-full border border-white/15 p-3 text-sm font-bold">← 이전 문제</button><button disabled={!keywordCorrect} onClick={() => setStep(3)} className="rounded-full bg-amber-300 p-3 text-sm font-black text-[#342612] disabled:opacity-30">다음 문제 →</button></div></> : <><QuestionHeading eyebrow="QUESTION 03 · PAPER EVIDENCE" title={mission.third.heading} accent="마지막 확인" description={mission.third.prompt} /><div className="mt-7 grid gap-3">{mission.third.options.map((option) => { const selected = thirdSelected.includes(option.id); const correct = thirdChecked && mission.third.correct.includes(option.id); const wrong = thirdChecked && selected && !correct; return <button key={option.id} onClick={() => toggleThird(option.id)} className={`flex items-center gap-3 rounded-2xl border p-4 text-left ${correct ? "border-emerald-300 bg-emerald-300/10" : wrong ? "border-red-300 bg-red-300/10" : selected ? "border-amber-300 bg-amber-300/10" : "border-white/15 bg-white/[.035]"}`}><i className="grid h-9 w-9 place-items-center rounded-full bg-white/10 not-italic">{correct ? "✓" : wrong ? "×" : selected ? "✓" : ""}</i><span><strong className="block text-base">{option.name}</strong><small className="mt-1 block text-sm text-white/40">{option.note}</small></span></button>; })}</div>{thirdChecked && <div className={`mt-5 rounded-xl p-4 text-sm font-bold ${thirdCorrect ? "bg-emerald-300/10 text-emerald-200" : "bg-red-300/10 text-red-200"}`}>{thirdCorrect ? "정답입니다! Paper evidence를 정확히 찾았습니다." : "선택한 항목을 paper에서 다시 확인해 보세요."}</div>}<div className="mt-5 grid grid-cols-2 gap-2"><button onClick={() => setStep(2)} className="rounded-full border border-white/15 p-3 text-sm font-bold">← 이전 문제</button>{thirdCorrect ? <button disabled={saving} onClick={() => void onComplete()} className="rounded-full bg-emerald-300 p-3 text-sm font-black text-emerald-950 disabled:opacity-40">{saving ? "저장 중..." : `MISSION ${mission.id} 완료 →`}</button> : <button disabled={thirdSelected.length !== mission.third.correct.length} onClick={() => setThirdChecked(true)} className="rounded-full bg-amber-300 p-3 text-sm font-black text-[#342612] disabled:opacity-30">정답 확인</button>}</div></>}</aside></section></main>;
}

function QuestionHeading({ eyebrow, title, accent, description }: { eyebrow: string; title: string; accent: string; description: string }) { return <div><p className="text-[10px] font-black tracking-[.17em] text-amber-300">{eyebrow}</p><h1 className="mt-3 text-3xl font-black tracking-[-.04em] sm:text-4xl">{title}<br /><em className="not-italic text-[#f2c66d]">{accent}</em></h1><p className="mt-4 text-sm font-semibold leading-7 text-white/50">{description}</p></div>; }
function C2Complete({ onMap, onProfile }: { onMap: () => void; onProfile: () => void }) { return <C2Shell><section className="mx-auto max-w-4xl rounded-[2rem] border border-emerald-300/25 bg-[#211c16] p-7 text-center sm:p-12"><div className="mx-auto grid h-20 w-20 place-items-center rounded-full bg-emerald-300 text-4xl font-black text-emerald-950">✓</div><p className="mt-6 text-xs font-black tracking-[.2em] text-emerald-300">CHAPTER 02 CLEAR · FEATURES UNLOCKED</p><h1 className="mt-3 text-5xl font-black sm:text-7xl">LABLOG<br /><span className="text-[#f2c66d]">OPEN</span></h1><div className="mx-auto mt-7 flex max-w-2xl items-center gap-5 rounded-2xl border border-amber-200/20 bg-amber-200/[.06] p-5 text-left"><Image src="/os-penguin.png" alt="OS Lab 안내 펭귄" width={100} height={100} className="h-24 w-24 object-contain" /><p className="text-base font-semibold leading-7 text-white/65">축하해요! 이제 <strong className="text-amber-200">Mission, Update, Feed, Team</strong> 기능을 사용하고 활동 포인트를 모을 수 있어요.</p></div><button onClick={onProfile} className="mt-7 w-full rounded-full bg-emerald-300 px-7 py-4 text-base font-black text-emerald-950 shadow-[0_6px_0_#26705a]">프로필로 돌아가기 →</button><button onClick={onMap} className="mt-4 text-sm font-bold text-white/40 hover:text-white/70">Chapter 2 다시 보기</button></section></C2Shell>; }
function C2Shell({ children }: { children: React.ReactNode }) { return <main className="relative flex min-h-screen overflow-hidden bg-[radial-gradient(circle_at_15%_10%,#4a3723,transparent_28%),linear-gradient(#17130f,#211a13)] p-5 text-white sm:p-8"><div className="pointer-events-none absolute inset-0 opacity-15" style={{ backgroundImage: "linear-gradient(rgba(242,198,109,.18) 1px,transparent 1px),linear-gradient(90deg,rgba(242,198,109,.18) 1px,transparent 1px)", backgroundSize: "42px 42px" }} /><div className="relative z-10 flex w-full">{children}</div></main>; }